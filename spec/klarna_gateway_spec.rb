require 'spec_helper'

describe KlarnaGateway do
  it 'has a version number' do
    expect(KlarnaGateway::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
